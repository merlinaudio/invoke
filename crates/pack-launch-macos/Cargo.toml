[package]
name = "pack-launch-macos"
version = "0.1.0"
edition = "2024"

[dependencies]
invoke = { version = "0.1.0", path = "../invoke" }
log = "0.4"
serde_json = "1.0.145"
tokio = { version = "1.48.0", features = ["io-util", "net", "rt", "sync", "time"] }
