{
	"name": "@merlin.audio/invoke-pack-runtime",
	"version": "0.0.0",
	"description": "Invoke pack runtime — loads pack code and serves the pack wire protocol; compiles to the invoke-pack-runtime binary the CLI spawns",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "bun build:proto-types & bun build:pack-types & bun scripts/build.ts",
		"build:proto-types": "cargo +nightly -Zscript scripts/proto-types/generate.rs src/proto.d.ts",
		"build:pack-types": "bun scripts/pack-types/generate.ts"
	},
	"dependencies": {
		"@merlin.audio/react-flight": "workspace:*",
		"@merlin.audio/router": "^0.2.0",
		"react": "^19.3.0-canary-fef12a01-20260413"
	},
	"devDependencies": {
		"@types/bun": "1.3.14",
		"@types/react": "^19.2.14",
		"rollup": "4.40.2",
		"rollup-plugin-dts": "6.2.1"
	},
	"peerDependencies": {
		"typescript": "^6.0.2"
	}
}
